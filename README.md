# Dependencies

```bash
 docker buildx build --platform linux/arm64,linux/amd64 \                                    
  -t registry.bun-company.ts.net:5000/haskell-home-controller:latest \
  --push .
```

